numbers = (1, 2, 3 ,4) # immutable
print(type(numbers))
# faster than lists
# makes code more robust
# valid keys in a dict
count_ones = numbers.count(1)
index_of_two = numbers.index(2) # will only give first matching index
